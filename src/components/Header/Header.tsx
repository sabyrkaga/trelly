import { PageTitle } from "../PageTitle"
import { ResetButton } from "../ResetButton"

export const Header = () => {
  return (
    <header>
      <PageTitle />
      <ResetButton />
    </header>
  )
}
